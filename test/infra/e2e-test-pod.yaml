apiVersion: v1
kind: Pod
metadata:
  name: e2e-test
  namespace: e2e-test
spec:
  serviceAccountName: e2e-test-sa
  restartPolicy: Never
  containers:
    - name: e2e-test
      # TODO: pull image from head or from image built from the branch being tested
      image: gcr.io/test-csi-test-infra/e2e-test:latest
